services:
  lgtm:
    image: grafana/otel-lgtm:latest
    container_name: lgtm
    ports:
      - "3000:3000"   # Grafana
      - "4317:4317"   # OTLP gRPC
      - "4318:4318"   # OTLP HTTP
    environment:
      - ENABLE_LOGS_ALL=true
      - ENABLE_METRICS_ALL=true
      - ENABLE_TRACES_ALL=true
